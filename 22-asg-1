#include<stdio.h>
void main()
{
	int n,f;
	printf("Enter the  n value");
	scanf("%d",&n);
	f=fact(n);
	printf("factorial = %d",f);
	getch();
}
int k;
int fact(int x)
{

	if((x==1)||(x==0))
	return k;
	else
	f=x*fact(x-1);
	return k;
}
